# Dataset oficial

Coloque nesta pasta o dataset oficial da FIAP com o nome exato:

```text
crop_yield.csv
```

Não utilize dados artificiais ou substitutos. O notebook da Fase 5 espera encontrar o arquivo em `../data/crop_yield.csv`, considerando sua execução a partir da pasta `notebook/`.
